export const getActiveIncidentsService = async () => {
    // Placeholder count for active incidents.
    // Replace with a real database query once the Incident model is available.
    return {
        total: 8,
    };
};
